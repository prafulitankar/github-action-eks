apiVersion: apps/v1
kind: Deployment
metadata:
  name: prft
  labels:
    app: prft
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prft
  template:
    metadata:
      labels:
        app: prft
    spec:
      containers:
        - name: prft
          image: 575108947352.dkr.ecr.eu-north-1.amazonaws.com/prftrepo:prftimg:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
